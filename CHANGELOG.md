# Supasito — build log

Session-by-session record of what was built, verified and fixed. The current plan lives in PLAN.md. The app was called Open until session 15 (2026-09-06); earlier entries use that name.

## Status (2026-09-03, end of first session)

Built and verified:
- Tauri app scaffolded, Rust core compiles, frontend type-checks, release bundle builds (`Open.app`, 10 MB binary, starter bundled as a resource).
- **End-to-end smoke test passed inside the real app** (`OPEN_SMOKE_PROMPT=… pnpm tauri dev`): dev server for a Next.js site started on a free port and became ready; a Claude Code session ran through the Rust bridge (Read → Edit → "Done. The hero headline is now 'Hello from Open'"); `git status` reported the change; clean shutdown.
- UI verified in the browser against the mock backend: rail, sessions, streaming transcript, tool step rows, element picker (click → chip in composer with tag/text), Enter to send, approval card with Allow / Always allow / Deny.
- Sites opened in Open are marked trusted in `~/.claude.json` so their `.claude/settings.json` allowlists apply (Claude Code ignores project permissions in untrusted folders).

Not yet verified by hand: the production build running as a real app (CSP + `tauri://localhost` iframe of `http://localhost:*`), the New-site dialog end to end, Publish against a real Vercel project.

Next: M1 leftovers (device widths and open-in-browser are built but untested in the app), M2 (undo a turn, publish log polish), then M3.

Fixed after first hands-on test (2026-09-03, later): Astro/Vite dev servers bind only the IPv6 loopback on macOS, so readiness now probes both `127.0.0.1` and `::1`, the free-port check is dual-stack, and the URL the server prints ("Local: http://localhost:4322") overrides the requested port if a framework moved. `tauri dev` relaunches the app on Rust changes without running shutdown hooks, which orphaned dev servers; Open now records their pids in `dev-pids.json` and reaps them on the next launch.

### Session 2 (2026-09-03, afternoon)
- **Undo a turn (M2):** the completion line now lists how many files the turn wrote and offers Undo, which restores tracked files with git and deletes files the turn created. Verified in the mock UI; the git logic has a unit test.
- **Picker verified on a live Next.js 16 page:** tag, classes, text, selector, computed styles and trimmed HTML come through; React Server Component owners (which have no client fiber) are now read from React 19's component-info objects so the component chain (`Hero`) is included. No file:line from Next, as expected.
- **Production build verified as a real app:** launched `Open.app` with an isolated home; the iframe loaded an Astro site on port 4322 under the release CSP and `tauri://localhost`, and the injected picker reported ready. D3 holds.
- **New-site path tested:** `create_from_starter` copies the starter, installs, inits git and commits (integration test, run with `cargo test -- --ignored`). Unit tests cover site detection, git restore, port parsing and session-dir encoding.
- Diagnostics: the app prints `[preview] …` lines to stderr when launched from a terminal, so preview loading can be checked without screenshots.

Still untested by hand: Publish against a real deploy target (needs your account), the New-site dialog UI itself (the Rust path behind it is tested).

### Session 3 (2026-09-03, evening)
Publish confirmed against Vercel by the user. Built next:
- **Images in messages (M3):** paste or drop PNG/JPEG/GIF/WebP into the composer; sent as base64 image blocks alongside the text and selection. Tauri's own drag-drop handler is disabled so the webview gets normal HTML5 drops. Thumbnails in the composer and in the user message; images in saved transcripts render too.
- **Queue while busy (M3):** the composer stays open during a turn; Enter queues (Claude Code processes queued input after the current turn) and the message shows a dashed "Queued" state until its turn starts. Stop stays available.
- **Publish command editor:** the Publish dialog now sets the command (presets: Vercel, Cloudflare, Netlify, git push) and writes it to `open.json`; "Change command" from the result view.
- **Exit notices** include the last stderr lines from Claude Code (e.g. not logged in).
- **App icon:** a bracket-and-lines mark in the accent gradient; source in `src-tauri/icons/icon.svg`, set generated with `tauri icon`.

Remaining from the plan: Windows build (unverified platform), optional second agent backend. Both deferred on purpose.

### Session 4 (2026-09-03, later)
Verified against the real Claude Code (2.1.257, haiku) with new smoke scenarios (`OPEN_SMOKE_SCENARIO=queue|interrupt|pointing`):
- **Queue:** a second message sent mid-turn is processed after the first; results arrived in order (ONE, then TWO).
- **Interrupt:** the Stop button's `interrupt` control request ends the turn with `result{subtype:"error_during_execution", is_error:true}`; the process stays alive and the next message works. The UI now shows such a turn as "Stopped" (muted) instead of a red error, and other error subtypes get plain-language text.
- **Pointing:** a message with an attached selection (the starter's `h1`, component chain `Hero < Page`, no file:line) made Claude read `app/page.tsx`, open `components/hero.tsx` and change only that headline. The selection block format works as designed without source stamping.
- ⌘⇧E toggles the element picker; shortcuts are listed in the README.

### Session 5 (2026-09-03)
- **The preview follows the page being edited:** an Edit/Write of a page file maps to its route (Next app and pages routers, Astro, SvelteKit, Nuxt; route groups and parallel routes stripped, dynamic segments skipped) and the preview navigates there. 19-case check in `src/routes.test.ts`, run by `pnpm test`.
- **Plan usage chip** from `rate_limit_event` (highest window utilisation, colour-coded from 75% and 90%, reset time in the tooltip).
- **Setup card** replaces the banner when Claude Code isn't found: install link, sign-in step, "Check again", "Set the path manually".
- **Reveal in Finder** on each site row; **error boundaries** around the three panes so a render error shows a retry card instead of a blank window.
- Mock preview reports `/` instead of `srcdoc` as its path.

### Session 6 (2026-09-03, user testing)
From the first hands-on round: pointing worked on a real site. Fixed: the user bubble lost its accent left border and the selection chip no longer wraps (it prefers a short plain class name, or none, and truncates); Publish now confirms first (command + changed-file count) and a running publish can be cancelled (the command runs in its own process group; `publish_cancel` stops it); Settings has a model dropdown (default / Sonnet / Opus / Haiku / custom). Also added: claude processes are recorded in `agent-pids.json` and reaped on the next launch, like dev servers, so an app relaunch during a turn leaves nothing running.

### Session 7 (2026-09-04)
- **Preview and production publish targets.** `open.json` gains `preview` next to `publish`; both are inferred per host (Vercel `vercel deploy --yes` vs `--prod`, Netlify `netlify deploy` vs `--prod`, Cloudflare `wrangler versions upload` vs `deploy`) and editable in the Publish dialog. The dialog is a two-card choice with the command shown under each.
- **Commit and push on publish.** Optional steps in the same dialog: commit all pending changes (message prefilled from the last request) and push to `origin` when a remote exists. Git identity missing → a plain-language error with the exact commands. Answers "does it back up to GitHub?": only when you tick push; nothing is pushed silently.
- **Diff per turn:** "N files changed" opens a unified diff (working tree vs HEAD, untracked files shown as additions, capped at 800 lines).
- **Dock badge and attention:** badge = pending approvals across sessions; the Dock bounces on a new approval request or a finished turn while the window is unfocused. No notification plugin.
- ⌘N new session, ⌘, settings.

### Session 8 (2026-09-04)
- **Transcript reducer tests** against a recorded Claude Code 2.1.257 stream (`src/agent/fixtures/`), plus streaming, interrupt, queue, files-touched and saved-transcript cases. `pnpm test` runs them with the route check and the Rust suite; test files are excluded from the app's type-check.
- **Show Claude the preview:** camera button → `preview_capture` asks WKWebView for a snapshot of the iframe's rect (`takeSnapshotWithConfiguration:` via objc2 + block2, width capped at 1600 px); the PNG is attached to the composer like a pasted image. Cross-origin iframe content is included and no Screen Recording permission is needed (a first attempt with `screencapture` needed that permission and was dropped). macOS only.
- **Release script** (`pnpm release [--install]`) so daily use doesn't depend on `tauri dev`.
- Rail: session list capped at 12 with "Show older"; "Open in your code editor" (code, cursor, zed, windsurf on PATH; Finder otherwise).

### Session 9 (2026-09-04)
- **Transcript performance:** items are now replaced, never mutated, and rows (`Entry`, `Steps`) are memoised, so a streamed token re-renders one row instead of the whole transcript.
- **Idle dev servers stop** when you switch sites, unless a session on that site is still working.
- **Undo is gated by commit time:** turns from before the last commit made in Open lose their Undo (git is the right tool from there).
- `CLAUDE.md` added for agents working on this repo: the one rule, how to run and test, where the sharp edges are.

### Session 10 (2026-09-04)
- **Site rules dialog:** edits the site's `CLAUDE.md` from the rail (template offered when missing). This is where Webflow's "Brand" lives in Open: a file the agent reads.
- **Rename** a site inline (writes `open.json` name).
- **Slash-command autocomplete** from `system/init.slash_commands` + `skills`; Tab or Enter inserts.
- **Per-session permission mode** via `control_request{subtype:"set_permission_mode"}` (smoke scenario `mode`); the header shows the current mode from `system/init.permissionMode`.

## Backlog (deferred on purpose)
- **Windows build.** Needs a Windows machine to test WebView2's mixed-content policy for the localhost iframe, the all-frames init script, process-group handling and the missing Dock APIs. Everything platform-specific already sits behind `#[cfg(target_os)]`.
- **Second agent backend** (e.g. Codex CLI) behind the same `agent://` events.
- **Session list caching** (`sessions.rs` re-reads JSONL heads on every site switch; fine until a site has hundreds of sessions).

### Session 11 (2026-09-05) — review
Own pass plus an independent reviewer agent over the Rust and TypeScript. Fixed:
1. **Undo could delete files it didn't create** (untracked, gitignored, or from an earlier uncommitted turn) and accepted paths outside the site. Now: Rust reports, before each `Write` runs, whether the file already existed (`agent://fs`); Undo restores tracked files, deletes only files the turn created, leaves other untracked files alone and says so; every path must resolve inside the site; Undo requires git.
2. **Cancelling a publish during commit or push let the deploy run** and then reported "cancelled". Now the flow checks for cancel after every step and stops before deploying; Cancel stays clickable during the deploy itself.
3. A dev server that never opened its port left "Starting…" forever → after 90 s it becomes an error with a hint and a retry.
4. A queued message ended the previous turn's file list early and lost its "Queued" tag on the next tool step → queued messages are skipped when collecting a turn's files and are unqueued when the turn's result arrives.
5. git commit/push/diff/status/undo and session listing ran on the UI thread (a slow push froze the window) → now off-thread.
6. Switching sites quickly could attach the old site's session and dev server to the new one → guarded after every await.
7. Double-clicking Send on a new session started two Claude processes → in-flight guard, attachments cleared synchronously.
8. Removing a site left its Claude sessions running with unreachable approvals → sessions are stopped and dropped first.
9. Escape never interrupted (docs said it did) → it does, after dialogs and picking.
10. New-site creation reported a git failure as an install failure and left a half-made folder → separate steps; install failure removes the folder; git is best effort.
11. Unanswered approvals stayed "pending" after the turn ended or the process left → they expire and the Dock badge clears.
12. Errors the CLI returns to interrupt / set-mode requests were dropped → surfaced as a notice.
13. The readiness probe kept running after a stop and could report a killed server as ready → it exits when the child is gone.
14. The dev-start reply could overwrite a newer status → newer status wins.
15. `~/.claude.json` was rewritten non-atomically → write-then-rename.
Also from my own pass: the preview iframe kept the previous site's page path; reload double-loaded; Stop escalation could signal a recycled pid; a small per-screenshot leak; the last dev port was never remembered; push without commit is now disabled.

### Session 12 (2026-09-05) — v0.2 items 2, 3, 4 (groundwork), 5 (partly)
- **First-run checks.** New `toolchain.rs` reports Node (`node --version`), the package manager New site will use (pnpm, else npm), git (asks `xcode-select -p` before touching `/usr/bin/git`, which on a bare Mac is a stub that pops the "install command line tools" dialog), and Claude Code with its sign-in state from `claude auth status` (JSON, exit 1 when signed out; verified on 2.1.257). The UI shows a checklist with one line per missing tool: as the session pane when Claude Code is missing or signed out, on the welcome screen when there are no sites and something is missing, compact in Settings. The preview's "dev server didn't start" and "install dependencies" cards say when the cause is a missing Node. The check runs off the boot path so the window opens at once. Mock: `?tools=missing|nologin|nonode|nogit|nopnpm`, `?sites=none`.
- **npm fallback.** New site picks pnpm if on PATH, else npm (`--no-audit --no-fund`), before copying anything so a missing Node leaves no folder behind; with npm the starter's `.claude/settings.json` rules and CLAUDE.md are rewritten (`pnpm typecheck` → `npm run typecheck`, `pnpm exec tsc` → `npx tsc`; unit-tested). Installer output now streams into the New-site dialog. "Install dependencies" for an opened site falls back to npm when its lockfile's manager isn't installed and says so.
- **Failure paths, recorded for real:** signed out, Claude Code `-p` does not fail on stderr; it emits a synthetic assistant message (`error: "authentication_failed"`, text "Not logged in · Please run /login") and a `result{is_error:true, subtype:"success", terminal_reason:"api_error"}`, then exits 1. The reducer now drops the synthetic bubble and rewords the result ("run `claude auth login`"); the store re-runs the toolchain check so the checklist takes over. Offline (`fetch failed`) and rate-limit texts are reworded by pattern (not recorded); a `rate_limit_event{status:"rejected"}` adds one notice with the reset time. A failed publish whose output mentions login/credentials/401 now says "run `<cli> login` in this folder"; a missing CLI says how to install it. Tests added for the signed-out stream, offline text and rate-limit notice.
- **Signing/notarization groundwork:** `scripts/release.sh` reads `.env.release` (gitignored), prints whether it signs (`APPLE_SIGNING_IDENTITY`) and notarizes (`APPLE_ID`+`APPLE_PASSWORD`+`APPLE_TEAM_ID`, or the API-key trio), takes `--dmg`, and verifies the signature after the build; `tauri.conf.json` sets a minimum macOS of 12 and the DMG layout. README has the steps. Not exercised with a real Developer ID yet.
- Verified: `cargo test` (toolchain and npm-rule tests, the toolchain test runs against this machine), `pnpm test`, `tsc`, and the four checklist states in the mock UI by screenshot.

### Session 12, continued — doing the "left for you" items that don't need a human
- **Offline, recorded for real** (`ANTHROPIC_BASE_URL` pointed at a closed port): the CLI emits `system/api_retry` ten times with growing delays (0.5 s → 38 s, about 3 minutes in total) and only then the synthetic assistant message + `result{is_error}` with "API Error: Connection refused — a firewall or proxy may be blocking it (ConnectionRefused)". Open showed nothing but "Working…" for those 3 minutes. Now the Working row reads "Can't reach Claude's API; retrying in 4s (3 of 10). Esc stops the turn." (also worded for rate limiting, overload and 5xx from `error_status`/`error`), and the final text is reworded with the CLI's hint kept. The earlier pattern missed "Connection refused"; the recorded text is now a test.
- **Rate limit:** not provoked (it would burn the plan). The CLI's own list of API error texts was pulled from the binary ("rate limited", "overloaded", "529", "usage limit reached", "credit balance too low", "Please run /login", "Invalid API key", "401"); the rewording covers each.
- **Toolchain check in the real app** (`OPEN_SMOKE_SCENARIO=tools`, new): all green on this Mac; `HOME=<empty>` → `loggedIn:false, authMethod:"none"`; `OPEN_PATH=/usr/bin:/bin` (new debug-only override in `login_shell_path`) → Claude Code and Node not found, no package manager. Found and fixed: Apple's `git version 2.50.1 (Apple Git-155)` was parsed as "Git-155)".
- **Review fixes exercised:** a dev command that never opens its port (`open.json` with `sleep 300`) became an error after 90 s with the hint, and the child was killed (real app, smoke run). Cancel during commit (mock, via `window.__store`): commit completed, push skipped, deploy never ran, dialog shows "Stopped before deploying."
- **Signing: dropped for now** ("underground release"). The keychain has only an *Apple Development* identity; Gatekeeper on other Macs needs *Developer ID Application*. `pnpm release --zip` writes `release/Open-<version>-macos.zip` and prints the one-line quarantine fix for recipients; README says the same. The signing path stays in the script for later.

### Session 13 (2026-09-06) — usage and context fullness
- **Usage ring by the composer.** A 14 px ring at the right of the composer bar fills with the conversation's context use; grey and unlabelled below 50%, then the percentage, amber from 75%, red from 90%. Click: a popover with "N% of Claude's working memory · Xk of Yk tokens", what happens when it fills (Claude Code summarises by itself; ⌘N for a clean slate), the plan's 5-hour and weekly windows with reset times and meters, and the cost so far. Settings gets a read-only "Plan usage" block from the last rate-limit event any session received. The header chip is unchanged (alarm above 50%).
- **How context is counted:** the last API call's `usage.input_tokens + cache_creation_input_tokens + cache_read_input_tokens`, the same rule as Claude Code's status line; `message_start` events update it as soon as a call starts, subagent calls and synthetic error messages are skipped. The window comes from the result's `modelUsage.<model>.contextWindow` (200k in every recording); before the first result it is 200k, or 1M for "[1m]" models. `system/compact_boundary` (stream-json `compact_metadata{pre_tokens, post_tokens?}`, saved JSONL `compactMetadata{preTokens, postTokens?}`) resets it and adds a notice; the Rust transcript loader now keeps those lines so a resumed session shows them.
- **Cost was a running total.** A two-turn probe against the real CLI (haiku, stream-json input) reported `total_cost_usd` 0.030972 then 0.0395562: cumulative for the process (the arithmetic matches haiku's 1-hour cache-write price), and 0 for an interrupted turn. The completion line now shows each turn's share; the popover shows the session total (since resume, when the transcript was re-rendered from disk). The same probe recorded the real `rate_limit_event` shape (`unifiedWindows.{five_hour,seven_day}.{utilization,resetsAt}` plus `overageStatus`), which the mock already matched.
- Verified: reducer tests for context, cost deltas, interrupt, process restart, both compaction shapes and the plan windows; `tsc`; `cargo build` + `cargo test`; in the mock (`pnpm dev`): ring at 31% on a resumed session, popover after a turn, `?context=full` for the amber/red states and the compaction notice, Settings block.

### Session 14 (2026-09-06) — exact model, effort, fast mode, reasoning
- **What the CLI does, verified on 2.1.257 with `-p` probes** (recordings in `src/agent/fixtures/claude-2.1.257-thinking-summaries.jsonl` and `…-fast-mode.jsonl`, both reducer tests now): thinking text is withheld in headless mode (`{"type":"thinking","thinking":""}`, `thinking_delta` without text, `system/thinking_tokens` estimates) unless `--settings '{"showThinkingSummaries":true}'` is passed, and then the thinking block streams *before* the text and arrives as its own assistant message. Fast mode has no flag: `--settings '{"fastMode":true}'`, Opus only; `system/init` and `result` carry `fast_mode_state` (`on|off|cooldown`) and `fast_mode_disabled_reason` (`sdk_opt_in_required` without the opt-in), `result.usage.speed` says whether the request ran fast. `--effort low|medium|high|xhigh|max` works but is not reported back. The exact model id is in `system/init.model`, every assistant `message.model` and `result.modelUsage`. `--settings` merges over the user's own settings. A one-word Opus fast turn cost $0.257 against $0.034 on Haiku.
- **Reasoning.** Open always asks for thinking summaries. The reducer now keeps the row streaming through the thinking-only assistant message (before, that message ended streaming and the text deltas that followed were dropped until the full text arrived), tracks the streaming `phase`, and the row shows "Thinking…" with the summary while it streams, then folds into the collapsed "Thinking" row once the answer starts.
- **Exact model.** Settings lists real ids (Fable 5.1, Opus 5, Sonnet 5, Haiku 4.5, custom name or alias) with a `[1m]` checkbox; "Your Claude Code default" names the `model` and `effortLevel` read from `~/.claude/settings.json` (toolchain.rs). The header chip became a select showing the id the CLI reported; assistant items carry `model`; a turn's completion line names the model when it differs from the session's (or several ran) and adds `fast` when `usage.speed` was fast.
- **Effort and fast mode**, in Settings (defaults) and the header (per session; fast only when the model is Opus, with `cooldown` shown). Rust: `Persisted.effort`/`fast_mode`, `--effort`, `--settings`, `Overrides` on `agent_start`, `set_model` and `apply_flag_settings` control requests (`agent_set_model`, `agent_apply_settings`, both return the request id). Store: a change on an idle running session sends the control request; a `control_error` for that id (or a choice with no request form, such as "back to default") stops the process so the next message resumes it with the new flags; drafts and stopped sessions remember the choice for their start. Mock: thinking stream, `fast_mode_state`, `?fast=on`.
- New smoke scenarios `model` (set_model between two turns) and `fast` (apply_flag_settings between two turns), `OPEN_SMOKE_EFFORT`, `OPEN_SMOKE_FAST=1`.
- Verified: reducer tests on both recordings plus synthetic cases (thinking phase and streaming, exact ids, fast on/off/cooldown, `usage.speed`); `tsc`; `cargo test` (extra settings JSON, overrides, user defaults). Mock (`pnpm dev`): "Thinking…" streams then folds, header chips, `?fast=on` shows `Fast · on` on Opus, a draft's model/effort choices reach the started session, Settings shows "Your Claude Code default · claude-fable-5-1[1m]" / "· high". Real CLI (scratch site, `OPEN_SMOKE_SITE_PATH`): scenario `model` (haiku → `set_model sonnet`): the next turn's `system/init` and `message.model` were `claude-sonnet-5`; scenario `fast` (opus → `apply_flag_settings {fastMode:true, effortLevel:"low"}`): the next turn's init and result reported `fast_mode_state: "on"` (the one-word answer still ran at `usage.speed: "standard"`, as in the probe). The two smoke runs cost about $0.30 and $0.46.
- **Layout (user feedback, same day):** the model / effort / fast / permission-mode chips moved out of the title bar into a bar under the conversation, above the composer (`.knobs`), so the title bar holds only the title and Stop. The header's plan-usage chip was a second usage indicator next to the composer ring and is gone; the ring now colours by the worse of context and plan usage and names a plan window past 75% ("plan 82%"), with the reset time in its tooltip.
- Then, per feedback: the chips became icon-led dropdowns (`Pick`): a robot icon for the model, signal bars for effort, the word "Mode" for the permission mode, each showing its current value; Fast unchanged. Order: model, effort, mode, fast.

### Session 15 (2026-09-06) — Supasito: the name and the icon
- **Renamed Open → Supasito** everywhere the user or an agent sees it: product name, bundle identifier (`co.wozu.supasito`), crate (`supasito` / `supasito_lib`), window title, rail brand, UI copy, the system prompt, the starter's copy and `CLAUDE.md`, release script (`Supasito.app`, `release/Supasito-<version>-macos.zip`), README, PLAN and this log's title. Also, after a second look: the per-site config is now `supasito.json` (read falls back to `open.json`, and the first write from Supasito renames the file), the starter package is `supasito-starter`, the dev knobs are `SUPASITO_SMOKE_*` / `SUPASITO_PATH`, the CLI entrypoint tag is `sdk-supasito`, and the picker's comments say Supasito. The app data dir follows the identifier, so `AppState::load` copies `co.wozu.open/state.json` into the new dir the first time it finds nothing there; older entries in this file keep saying "Open".
- **App icon** from the Icon Composer document made for the app (`src-tauri/icons/Supasito.icon`, coral automatic gradient, warm pale glass "s", 65% translucency, neutral shadow, group opacity 75% with 100% in dark and tinted). `pnpm icon` (`scripts/icon.sh`) compiles it with Xcode 26's `actool` into `icons/Assets.car` (bundled as a resource; `src-tauri/Info.plist` merges `CFBundleIconName: Supasito`), pads Icon Composer's 1024px default export (`icons/default-1024.png`) to the macOS icon grid (864 px of art on 1024, the inset actool itself uses) with `scripts/icon-pad.swift`, and runs `tauri icon` for the flat PNG / icns / ico set. The rail's brand dot became `Mark`, the same glyph flat on the coral tile.
- Verified: `tsc`, `pnpm test` (14 cargo tests + reducer/routes), mock UI in the browser (mark, title, placeholders); `pnpm tauri build --bundles app` produced `Supasito.app` with `CFBundleIdentifier co.wozu.supasito`, `CFBundleIconName Supasito`, `CFBundleIconFile icon.icns`, `Resources/Assets.car`; `NSWorkspace.icon(forFile:)` on the bundle returns Apple's rendering of the layered icon (inset plus the renderer's drop shadow, which the flat icns does not have), so macOS 26 reads the Assets.car. Not verified: the dark and tinted appearances on a real Dock, and a pre-26 macOS falling back to the icns.
- The repository moved to GitHub as `wojtekwoz/supasito` (public). No LICENSE file yet.

### Session 16 (2026-09-06) — full-width preview
- **The preview can take the whole window.** A button at the left of the preview toolbar (arrows pointing out; pointing in while the preview is wide), or ⌘\, hides the rail and the conversation so the page fills the app (`previewFull` in the store, `.app.full`). Built as an overlay rather than a grid change: the preview pane becomes `position: fixed; inset: 0` and the two left panes turn `visibility: hidden` in place, so they keep their layout, scroll positions and streaming, the iframe never remounts, and the title bar gains the traffic-light padding the rail normally holds.
- **The conversation comes back by itself when it is needed:** picking an element (the selection chip lands in the composer), ⌘N (a new session is for talking), and an approval request for the current session (a card behind the preview would stall the turn unseen). Entering full mode blurs whatever had the keyboard, so typing does not land in the hidden composer.
- Verified in the mock (`pnpm dev`, driven from the browser): the button and ⌘\ toggle both ways; the iframe's window survived the switch (a marker set on `contentWindow` was still there); the title bar padding was 84 px in full mode; picking the headline exited full mode with the `h1` selected; ⌘N exited into a draft; the mock's approval card exited full mode as it arrived, while a card for a session that is not current leaves it alone. `tsc`, `pnpm test`. Not verified in the Tauri window: that ⌘\ reaches the page (no default menu item claims it).
- **The knobs row (model, effort, mode, fast) moved under the composer**, the last thing in the session pane; it had sat between the conversation and the input. `.knobs` lost its top border and tucks 4 px under the composer. The full-width button uses expand/collapse arrows (`Expand`/`Collapse` in `Icons.tsx`, drawn like the macOS full-screen control) in place of the sidebar glyph, which is gone.
- **README rewritten as a user-facing guide**: who it is for, what to install, install from the zip (with the quarantine step) or from source, the first five minutes, everyday use, what Supasito does on your Mac, what to expect, troubleshooting, uninstall; the developer notes moved to a short section at the end.

### Session 17 (2026-09-06) — acceptance checks: npm, undo, site switching
- **New site without pnpm, verified.** `cargo test -- --ignored create_site_from_starter` with `SUPASITO_PATH=<node, npm, npx, git only>:/usr/bin:/bin` picked npm (`npm install --no-audit --no-fund --loglevel=error`, 47 packages), wrote `package-lock.json` and no `pnpm-lock.yaml`, and rewrote the starter's `.claude/settings.json` (`npm run typecheck`, `npx tsc *`, `npm run build`, `npm run lint*`) and CLAUDE.md (`npm run typecheck`); the site's `node_modules/.bin/next dev -p 4873` answered HTTP 200 within 2 s on that same PATH. With pnpm back on PATH the same test still picks pnpm (`pnpm-lock.yaml`, no `package-lock.json`). No code change; PLAN §8.3 closed.
- **Undo verified against the real CLI.** New smoke scenario `undo` (`SUPASITO_SMOKE_SCENARIO=undo`, needs a clean git repo as the site): one haiku turn Writes `smoke-undo.txt` and Edits `components/footer.tsx`, then the scenario calls the same `undo_files` the Undo button uses — `files` from the turn's tool_use blocks (Edit/MultiEdit/Write/NotebookEdit), `created` from `agent://fs` — and asserts the restore report, `git status` clean and the created file gone. `site_undo_files` is now a one-line wrapper over `pub(crate) undo_files` (the `agent_start`/`start_agent` split), so the smoke exercises the command's exact path. The run: haiku, $0.10, 4 API turns; tracked file restored to HEAD, created file deleted, nothing skipped, repo clean afterwards. The UI-side guards in `undoTurn` (isGit, committedAt, markUndone) remain mock-verified only. Observation: no permission request was raised for Write/Edit; the CLI warned it ignores the starter's `.claude/settings.json` allow entries because the scratch folder is not trusted (the smoke inserts the site in memory, so the D8 trust marking did not run). Not an undo bug.
- **Fast site switching: a real race, fixed.** The mock gained `?sites=two` with per-site sessions and dev servers shaped like the Rust registry (start token, remove-before-kill, late `stopped`, one port per site reused on restart; `window.__mock.devs`), plus `?devDelay=<ms>` and `?stopDelay=<ms>`. With `devDelay=300&stopDelay=1500`, switching back to a site whose server was still being killed let the old server's `stopped` event overwrite the restarted one (same port): A starting → ready at 2001 ms → old `stopped` at 2011 ms; the preview showed "The dev server stopped … Try again" while the registry had A ready. The store now keeps a `devStops` map (`stopDevServer`): `selectSite` waits for a pending stop before asking that site's status, a stop issued twice is one stop, `stopDev`/`restartDev`/`removeSite` go through the same map, and a server that finished spawning after the user left (no busy session) is stopped. Same timing after the fix: stopped → starting → ready, no overlay. Burst B,A,B,A with no gaps: one live server (A), B never started (abandoned at the stale-switch guard), nothing in starting; sessions, current session, previewPath, selection and git all A's. A busy session on A keeps A's server when switching to B (two live servers), as documented. Not tried in the Tauri window.
- **⌘\ matched by key position** (`e.code === "Backslash"`; `altKey` no longer excluded) so it fires on layouts where backslash needs Option (German, French). Checked with synthetic keydowns in the mock; the browser harness cannot inject `code`, real keyboards do. Whether it reaches the page in the Tauri window was settled by reading, not pressing: lib.rs builds `tauri::Builder::default()` with no `.menu()` and tauri.conf.json has no menu, so Tauri's default macOS menu is installed (tauri 2.11.5 `Menu::default`); its accelerators (muda 0.19.3) are ⌘C ⌘X ⌘V ⌘Z ⇧⌘Z ⌘A ⌘M ⌃⌘F ⌘H ⌥⌘H ⌘W ⌘Q and macOS's own Window items are unbound, so nothing claims Backslash and the keydown reaches WKWebView by the path ⌘N already takes there. Not pressed in the real window yet.
- `tsc`, `pnpm test`, `cargo build` clean.
- **Plain Vite (8.2) and SvelteKit (kit 2.63) exercised from fresh scaffolds.** Both bind `[::1]` only; the dual-stack probe and the printed `Local:` port make readiness pass. Vite's "Port N is in use, trying another one" then `Local: http://localhost:N+1/` fallback is picked up by the marker scan and moves the preview to the new port. `supasito.json` is not written on open (only when a command is set or the site is renamed), as intended.
- **Detection bug found by that run, fixed.** `refresh` looked dependencies up with `pkg.pointer("/devDependencies/<name>")`; a JSON Pointer splits on `/`, so `@sveltejs/kit` was never found and a SvelteKit site was detected as plain `vite` (worked by accident: bare `vite` serves it). Now a plain key lookup under `dependencies`/`devDependencies`; the `/scripts/dev` pointer is a fixed path and stays. New test `detects_scoped_packages_like_sveltekit` (package.json with `@sveltejs/kit` + `vite` in devDependencies and `node_modules/.bin/vite` present → framework `sveltekit`, dev `node_modules/.bin/vite dev --port {port}`).
- `src/routes/about/+page.server.ts` now follows to `/about` (the load function belongs to that page); `+layout.*` and `+server.ts` stay null. Route cases added for all three.
- Noted: SvelteKit's scaffold is `engine-strict` and refuses Node 23 at install (`vite-plugin-svelte` wants ^20.19 || ^22.12 || >=24); the dev server itself ran on 23.
- **Nuxt 4 (4.5.2) exercised from a fresh `nuxi init`.** Detected as nuxt with `node_modules/.bin/nuxt dev --port {port}`; binds `[::1]` only; `Local:` line and listener at 12 s cold (0.3 s warm), first page in 0.35 s, inside the 90 s deadline. On a busy port Nuxt prints "Using alternative port 3000" then `Local: http://localhost:3000/`, which the marker scan follows (the get-port line has `"localhost")`, no colon). A second `nuxt dev` in the same folder exits with "Another Nuxt dev server is already running", so orphan reaping matters there; `reap_orphans` matches its cmdline. Follow-the-page missed `app/pages/*.vue` (Nuxt 4's srcDir); the Nuxt rule now takes an optional `app/` prefix, with cases (`app/pages/about.vue` → `/about`, `app/pages/index.vue` → `/`, `app/pages/blog/[slug].vue`, `app/app.vue`, `app/layouts/default.vue` → null). The Next App Router rule runs first and needs a `page.(tsx|…)` file, so it cannot claim these.
- **Monorepo (pnpm workspace, `apps/web` = the Next starter) exercised.** Per-package `.bin/next` exists after a root install, dev server `Local` in 1 s, `pnpm install` from the package installs the workspace. But `is_git`, `git_status` and `git_restore` gated on `<site>/.git`, so a workspace package showed no pending count and Undo failed, and its package manager read as npm (lockfiles were looked for in the site folder only). Now: `git_toplevel` (nearest `.git` ancestor, site included) decides `is_git` and the gates; status runs `status --porcelain -- .` from the site and strips `rev-parse --show-prefix` from each path, so `files` stay site-relative (undo and diff already resolved their paths against the site and run `git -C <site>`, which works from a subfolder); commit stages `add -A -- .`, the same set the count showed; package manager from the nearest lockfile walking up to the git top (or the filesystem root without git), same precedence. Test `workspace_package_is_scoped_to_its_folder` (temp repo with `apps/web`, edit inside + untracked file outside → `changed == 1`, `files == ["a.txt"]`; restore → clean; commit leaves the outside file untracked; `pnpm-lock.yaml` at the workspace root → pnpm, one above the repo → ignored). Opening the workspace root itself detects no dev command; the message now adds "This looks like a workspace root; open the app's own folder (for example apps/web)." when `pnpm-workspace.yaml` or `package.json.workspaces` is there.
- `cargo build`/`cargo test` (17 passed), `tsc`, `pnpm test` clean.
- **Double start on a fast switch, fixed in both layers.** Review found a second race behind the one above: with A ready and no busy session, B, A, B, A registers one pending stop for A (`st.dev[A]` stays `ready` until the kill ends, so both stops return the same promise) and two `selectSite(A)` waiting on it; when it settled both passed the `currentSiteId === id` guard and both called `startDev(A)`, two `dev_start` invokes milliseconds apart. `Registry::start` checked `status()` and then inserted under separate locks, so both saw nothing and both spawned; the second insert replaced the first `Running`, leaving a live server no stop, `stop_all` or the pids file knew about. Store: `selectSite` takes a generation (`selectGen`) and bows out at each resume point when a newer call exists. Rust: the check and the insert now happen under one map lock; a placeholder entry (pid 0, its own lock held until the child is recorded) claims the slot before spawning, so the second start returns the first's info once it has a pid, `stop` on a start still in flight waits on that lock and kills the real process, a failed spawn hands the slot back, and `kill_group(0)` is refused (it would have signalled our own group). A dead or timed-out server is still replaced, killed first. Mock: `window.__mock.devStartCalls` counts `devStart` invocations per site. With `?sites=two&stopDelay=3000&devDelay=300` and the burst at 100 ms gaps: 2 `devStart` calls for A before the store fix, 1 after; both times one live server (A ready on 3000, B stopped), A's sessions and session current. No Rust unit test: `start` needs an `AppHandle` and a real process.
- **⌘\ also matched by the character.** `e.code === "Backslash" || e.key === "\\"`, meta/ctrl and the shift guard unchanged. On German and French Mac layouts no key at the US backslash position produces `\` (it is `#` or a backtick) and the real backslash is ⌥⇧7 (`code: "Digit7"`), which the position match never saw; the comment now says so instead of claiming the position match covers those layouts. Synthetic keydowns in the mock: `{key:"\\", code:"Digit7", meta}` and `{key:"#", code:"Backslash", meta}` both toggle, ⌘7 does not, and the shifted chord still stops at the shift guard — so ⌘⌥⇧7 itself is not reached; lifting that guard for the `key` branch is a one-line follow-up if a German keyboard confirms the need.
- **⌘\ shift guard now applies to the position match only** (`(e.code === "Backslash" && !e.shiftKey) || e.key === "\\"`): the German/French backslash is ⌥⇧7, which sets `shiftKey`, so the previous guard filtered the very chord the `key` branch was added for. A literal `\` is accepted whatever modifiers produced it; ⌘| (⌘⇧\ on US) still falls through untouched. Review of be1f3b0/97607e1 found nothing else to fix: every registry path takes the map lock before an entry lock or an entry lock alone (`start` drops the placeholder before touching the map on both the spawn-error and success paths; the readiness probe never removes map entries), `persist` only writes entries with a child, and `--show-prefix` agrees with the porcelain paths under a symlinked `/tmp` (probed with git 2.51). Porcelain quoting of paths with spaces defeated the prefix strip for a workspace package, as it already defeated diff/undo for a root site — pre-existing, fixed in the next bullet.
- **Paths with spaces in `git_status` fixed.** Porcelain v1 quoted them (`"sub dir/my file.txt"`), so the prefix strip missed and diff/undo got a name matching nothing — Undo silently skipped those files. Status now runs `--porcelain -z` (NUL-separated, never quoted); a rename or copy entry (`R  new\0old\0`) keeps the new path only, the file that exists in the working tree and that diff and undo act on; `changed` stays the entry count. Test `status_paths_with_spaces_and_renames` (workspace package: edited `sub dir/my file.txt`, untracked `new file.txt`, `git mv a.txt b.txt` → `changed == 3`, three plain site-relative names; `git_diff` on the spaced file is non-empty; `git_restore` brings the content back).
- Reviewed a825ee3 and the rest of the session-17 diff; the `-z` parser, `wait_turn_files`, the `site_undo_files` wrapper, the route rules and the workspace-root hint held (a rename across the site boundary comes back as `D`, pruned by the pathspec before rename detection). Fixed the `?sites=two` mock: `siteRename`, `siteSetPublish`, `siteRefresh` and `siteInstall` answered with the first site whatever id they got, so renaming the second site left two `site-demo` rows in the rail; they go through `siteOf` now.

### Session 18 (2026-09-07) — a taken port
- **The screenshot.** "The dev server stopped" with Next's `listen EADDRINUSE: address already in use :::4321` under it. Two faults behind it. `port_free` bound `127.0.0.1` and `::1` to test the port, and both binds succeeded next to a Node server on the wildcard `::` (what Next listens on) because Node sets SO_REUSEADDR and so does std, which on macOS lets a specific-address bind coexist with a wildcard one (checked with Python: bind OK, connect OK). So the app handed Next a taken port. Then the readiness probe saw *something* answering on 4321 and reported ready, which is why the preview showed the stranger's server and the card said "stopped" rather than "didn't start". The holder on this Mac was `python3 -m http.server 4321` from session 12's smoke site, re-parented to launchd, which the orphan reaper had skipped because its command line has neither `dev` nor `node` in it.
- **Port check is bind + connect.** A port is free only if both loopback binds work *and* nothing accepts a connection on either address (`accepts`, 300 ms timeout; a loopback self-connect, which TCP allows on a listener-less ephemeral port, is discarded by comparing local and peer address). Unit test binds `[::]:0` and expects the port to count as busy. macOS tears a closed listener down a few milliseconds late, so the test polls for "free again".
- **Readiness checks who answers.** When the port answers and the server has not printed its address, one `lsof -iTCP:<port> -sTCP:LISTEN -Fpgc` names the listener; it counts only if its process group is the one we spawned (`process_group(0)` makes the child the leader). A foreign listener is logged ("Port 4321 answers, but it is node (pid …) in …; waiting for this site's own server"), and readiness waits for the `Local:` line or the exit; the 90 s message says so too.
- **Retry on the port we chose.** A server that dies after an EADDRINUSE-style line (Node's `:::4321`, Vite's `Port N is already in use`, Python's unnamed `[Errno 48]`, resolved to whichever port is actually busy) on the port we gave it is respawned on another free port, at most twice, status still `starting`, log line "Port 4321 is taken by node (pid …) in …; trying 4322." The exit path waits for the log pumps to drain first, so the last stderr lines are seen before the exit is judged.
- **Port problem card.** A conflict on a port the command chose itself (no `{port}` honoured, or the retries fail the same way) ends in `error` with `problem: { kind: "port", port, holder }`; the holder carries pid, pgid, name, command, cwd, and the site id when it is another Supasito site's server. The preview card becomes "Port 4321 is taken": "*next-server (v16.3.3)* (pid …) started in ~/Sites/other-site is listening on port 4321, and this site's dev command uses that port. Stopping it ends that program." / "Your site *Second* is using port 3000. Both sites' dev commands ask for that port, so only one can run at a time." / an unknown holder gets the `{port}` hint. Buttons: **Stop it and try again** (`dev_free_port`: another site is stopped through the registry; anything else gets SIGTERM on the listener plus the parents in its process group that would respawn it — `next dev` restarts its `next-server` — stopping at any shell, then SIGKILL after 2 s; the card is refreshed instead if the port changed hands) and Try again. The chip reads "Port taken".
- **Orphan reaper matches the recorded command** (`looks_like_ours`): the port in the command line and either the old dev/node heuristic or the arguments we ran, since `zsh -lc` execs a lone command and `python3 -m http.server 4321` shows up as Python.
- **Verified against the real supervisor**: new smoke scenario `ports` (`SUPASITO_SMOKE_PROMPT=- SUPASITO_SMOKE_SCENARIO=ports ./src-tauri/target/debug/supasito`) starts a Node holder on `::4321` and two scratch sites: the `{port}` site skipped to 4322; forced onto 4321 (`SUPASITO_SMOKE_FORCE_PORT`, read only by `devserver::start`) it retried onto 4322 with the holder named in the log; the fixed-port site ended in the port problem naming the Node pid, `free_blocked_port` reported "Stopped node (pid …)", the holder was gone and the site came up on 4321. 0 failures. Mock: `?dev=taken`, `?dev=taken:unknown`, `?dev=taken:site&sites=two`; all three cards checked in the browser, "Stop it and try again" led to a ready preview. First version of the card crashed the pane (a selector returning a new closure each render loops zustand); fixed.
- `cargo test` (23 passed, run eight times for the port test), `pnpm exec tsc --noEmit`, `pnpm test` clean.
- **A folder with no dev server is the first task, not a dead end.** Plain HTML (no package.json, no framework) used to hit "No dev server found" and stop. The card is now "This site can't be previewed yet" with **Set up the preview**, which sends a fixed prompt (`SETUP_PREVIEW_PROMPT` in store.ts): look at what is here, add a `dev` script that serves the site with live reload (Vite for plain HTML; Supasito appends `--port <n>`, or `supasito.json` with `{port}` when the server can't take that), install so node_modules exists, add it to .gitignore, one-line reply. While the turn runs the card says "Setting up the preview". On every `result` for a site without a dev command or with `needsInstall`, the store re-detects the site (`site_refresh`) and starts the dev server if that made it previewable; an install still missing shows the existing install card. Nothing new in Rust. Mock `?dev=none`: the site has no dev command until one turn has run; checked in the browser end to end (card → prompt in the transcript → Allow → result → Vite command detected → preview ready).

### Session 19 (2026-09-07) — the conversation floats over the full-width preview
- **In full-width mode the conversation no longer hides: it floats over the bottom-left corner**, as minimal as it gets. It is the same session pane restyled (`.app.full .session`: `position: fixed`, 360 px wide, at most 420 px tall or the window minus the toolbar, translucent pane colour with a backdrop blur), so the transcript, its scroll position, streaming, the draft and the chips carry over. Gone inside the panel: the title bar (the composer already has Stop), the knobs bar and the composer's hint text; model, effort, mode and other sessions need the full layout, one click or ⌘\ away. The composer takes the keyboard on entering the mode and the transcript re-sticks to its end (`previewFull` joined the stick effect's dependencies).
- **It stays out of the way.** While the picker is armed the panel fades to a quarter and lets clicks through (`.app.picking`); for a screenshot capture `capturePreview` sets `capturing`, waits two frames (or 250 ms, since frames stop while the window is occluded) and clears it after the shot; with the dev log open the panel moves up by its height (`.app.devlog`). App.tsx puts those three classes on the root next to `full`.
- The session 16 auto-exits (picking, ⌘N and an approval request leaving full-width mode) are removed: what they brought back is now visible. `setPreviewFull` no longer blurs the active element.
- **The panel can be dragged anywhere over the page** by a grip along its top edge (a 36 × 4 px pill; pointer capture on the grip, a full-window shield above the iframe while dragging, since the page would otherwise select text as the pointer crosses it). The position is kept as `panelPos` (left and bottom offsets; null = the corner), remembered while the app runs and applied again on re-entering the mode; `clampPanel` keeps the panel 16 px inside the window and below the toolbar on every move, window resize and panel resize (a ResizeObserver: a short draft panel dragged to the top is pushed down when the reply makes it taller). Double-click on the grip puts it back in the corner. Verified in the mock with a real pointer drag: the panel followed, listeners inside the iframe counted zero pointer or mouse events and no selection, growth at the top edge kept the panel below the toolbar, the 980 × 620 window clamped it inside, the position survived leaving and re-entering the mode.
- **Minimise and exit from the panel.** Two 18 px buttons at the right of the grip: − minimises the panel to a 36 px pill at the same spot (`panelMin`; the pill carries a status dot, accent and pulsing while Claude works, amber while an approval waits; click to restore, and the composer takes the keyboard again), the inward arrows leave full-width mode. Pointer-downs on the buttons do not start a drag. A minimised panel comes back by itself for an approval request in the current session, a picked element and ⌘N (the session 16 rules, now applied to the pill instead of the mode); entering full-width mode always starts with the panel shown. Verified in the mock with real clicks: minimise → pill at the corner with the transcript and composer gone; click the pill → panel back, textarea focused, transcript at its end; the arrows button → the three-column layout; the mock's approval card, a picked card and ⌘N each restored a minimised panel; leaving and re-entering the mode showed the panel.
- **The empty-session notice is gone** ("Describe a change, or switch on the picker…"): the composer's placeholder and its hint line say the same, and in the floating panel the notice was most of the height. A new session is the composer alone.
- Verified in the mock (`pnpm dev`, driven from the browser): entering the mode gives a 360 × ~220 px panel on a resumed session with the textarea focused and title bar / knobs / hint gone; a message typed there streams in the panel and the mock's approval card arrives inside it, scrolled into view; with the picker armed the panel's pointer events are off, a card under it gets picked and the chip lands in the panel's composer, the panel's opacity transition running 1 → 0.25; the dev log pushes the panel up 180 px; a capture toggles `capturing` around the shot and the attachment appears; at the 980 × 620 minimum window the panel stays inside the window; ⌘N opens a draft in the panel. `tsc`, `pnpm test` (23 cargo tests). Not verified in the Tauri window: the blur over the iframe and a real capture without the panel.
- **Settings → Interface: hide what you don't use.** Settings has two tabs now, Claude (the old contents) and Interface: a checklist of the interface's optional parts, grouped by where they sit. Conversation: the model/effort/mode/fast chips, the usage ring, the picker button in the composer, the keyboard hint, the tool-step rows ("What Claude did"), the Thinking row. Preview toolbar: full width, device widths, pick, reload, open in browser, screenshot, dev log, the status chip. Sidebar: the editor and Finder buttons on each site. Publish, the composer, the transcript and the path field stay: they are the loop. Unticking applies at once (the dialog's backdrop shows the change), "Show everything" resets, Done closes; the list is `hidden` in the persisted state (`settings_get/set`, `Persisted.hidden`, serde default so old state files load). The catalog and the `useShown(key)` hook live in `src/app/ui.ts`; components gate on it. Hiding the dev-log button also closes the log (there would be no way to close it otherwise); a hidden hint still shows while an image is being dropped or the picker is armed; ⌘⇧E and ⌘\ keep working with their buttons hidden. Verified in the mock: seven items unticked disappeared live behind the dialog (knobs, ring, step rows, device segment, camera, terminal button, site tools), `settings.hidden` held the seven keys, Escape closed the dialog, the Claude tab was unchanged, no console errors. Second pass, driven through `window.__store`: all fifteen keys hidden left the toolbar with the path field and Publish only, the composer bar with Send only, the rail row with its rules and remove buttons, and the open dev log closed; ⌘\ and ⌘⇧E still toggled with their buttons gone; the hidden hint came back with the picker's text while picking and left again; on a live turn (two tool steps, a Thinking row, an approval card waiting) the `steps` and `thinking` toggles removed and restored those rows while the messages and the card stayed; with `?tools=missing` the dialog still had both tabs and all fifteen boxes. Review fix: the dialog selected `settings.hidden ?? []`, a fresh array per render that zustand v5 can loop on; it selects the field and falls back to one module-level empty list. Rust test `hidden_defaults_and_round_trips`: a state file without `hidden` loads with nothing hidden, and the list survives serialise/deserialise. `tsc`, `cargo test` (25 passed), `pnpm test` clean.

### Session 20 (2026-09-07) — Interface defaults and a confirm on "Show everything"
- **A fresh install starts with five things hidden:** the model/effort/mode/fast chips, the picker button in the composer, the keyboard hint, the dev log button and the dev server status chip. The list is `Persisted::default_hidden` in src-tauri/src/state.rs; `hidden` gets a field-level serde default so a state file without it (a fresh install, or one from before Settings → Interface) loads with those five hidden, while an explicit `[]` (the user's "Show everything") stays empty. `Persisted` implements `Default` by hand for the same reason. `DEFAULT_HIDDEN` in src/app/ui.ts mirrors the list for the mock backend.
- **"Show everything" asks first.** A native confirm ("Tick all 5 boxes you have unticked and show everything?"), the same pattern as Undo (Remove site got its own dialog in session 21); Cancel leaves the list as it was.
- Verified in the mock: Settings → Interface opened with exactly those five boxes unticked and the ten others ticked, matching the intended first-run picture; "Show everything" raised the confirm with the count, Cancel kept `settings.hidden` at the five keys, OK emptied it and the button went away; no console errors. `hidden_defaults_and_round_trips` covers the missing-field default, the explicit `[]`, and the round trip. `tsc`, `cargo test` (24 passed, 1 ignored), `pnpm test` clean.

### Session 21 (2026-09-07) — Remove site asks in its own dialog, and a plan for favourites
- **The trash button in the rail opens a dialog instead of a native confirm.** "Remove ClarityOps from the sidebar?", then what that means: the site only leaves the list, nothing is deleted, the folder stays with its code, git history and Claude Code sessions, and the folder button brings it back; the path in mono under the text. When the site has a running session or a dev server the dialog adds "Supasito will stop its dev server first" (or the session, or both). Cancel, backdrop click and Escape close it; the primary button holds focus. `RemoveSiteDialog` in src/app/Dialogs.tsx, `removing`/`askRemoveSite` in the store, `removeSite` clears `removing` before it works. The tooltip now says "Remove from the sidebar (the folder stays on disk)".
- **Site switcher with favourites** (PLAN.md §8b). A star on hover on each site row (filled and always shown once set) keeps the site at the top of the rail; with any favourite set the rail shows the starred sites plus the current one, otherwise every site as before. An "All sites… (N)" row, shown when the rail hides something or past six sites, and ⌘⇧O open a dropdown under the titlebar: a filter field with focus (name or folder), favourites first, then the rest by last opened, each row with its path, star and trash (the same Remove dialog), and Open a folder / New site at the bottom; ↑↓ and Enter pick, Escape or a click outside closes. Persisted on `Site` in src-tauri/src/sites.rs as `favorite` and `last_opened` (serde defaults, so old state files load unchanged; `site_favorite_defaults_and_round_trips`), written by the new `site_favorite` and `site_opened` commands; the UI keeps `siteMenuOpen` in the store, `SiteMenu` lives in src/app/Rail.tsx, the mock has `?sites=many` (fourteen sites, three starred).
- Verified in the mock with `?sites=many`: the rail showed Acme Docs, Marrow, Rivet, the current ClarityOps and "All sites… (14)" with Sessions visible and no scrolling; the dropdown listed favourites first then the rest by last opened with the filter focused; typing "cl" left ClarityOps; Escape closed it; ⌘⇧O opened it; starring Northwind from the store moved it into the rail and to the top of the menu; ↓×5 and Enter selected Blue Harbor, closed the menu, stamped `lastOpened` and swapped it in for ClarityOps; unstarring two left the rail with the remaining favourites plus the current site; with `?sites=two` and nothing starred the rail was the old two rows and no "All sites…" row. No console errors.
- Run in the real app (`pnpm tauri dev` against the user's six sites): starring three from the rail persisted to state.json with `lastOpened` stamps and the rail showed them plus "All sites… (6)". The names had truncated to "vibec…" because the hover-only row buttons still reserved their width; they are now `display: none` until hover (the set star always shows), so a name gets the whole row.
- Verified in the mock with `?sites=two`: the dialog opened from the trash on hover with the right name, path and the dev-server line (the mock's server is `ready`); Escape closed it with `removing` back to null and both sites still there; the primary button removed the site, closed the dialog, stopped that server and selected the second site; no console errors. `tsc` and `pnpm test` clean.

### Session 23 (2026-09-08) — the palette follows the icon, and clears WCAG AA
- **The accent is now the app icon's coral orange.** `src-tauri/icons/Supasito.icon` declares its fill as extended-sRGB `1.0, 0.267, 0.173`; the compiled 1024 px export gradients #FF7963 at the top to #EC543B at the bottom. Two tokens come out of that: `--accent: #FF7A5E`, the tone for text, links, carets, spinners, the meter fill and the picker chips (≥6:1 on every surface), and `--accent-2: #FF5A3C`, the button fill. The old indigo (`#6F88FF` / `#5570F0`) is gone.
- **Orange fills carry near-black ink, not white.** White on #FF5A3C is 3.10:1 — it fails body text — so `--on-accent: #1A0F0B` (6.07:1) is what `.btn.primary`, `.send`, the Publish badge and the light overlay's primary button use. That was already a defect before the recolour: white on the old #5570F0 was 4.23:1.
- **The ground was re-derived from the accent** rather than left cool under a warm accent: rail #100E0D, pane #171514, elev #1F1C1A, elev-2 #272321, lines #2E2A27 / #3D3733, ink #EFEAE6 / #B7ADA5 / #9C938B. Same value ladder as before, hue mirrored to the warm side at half chroma, so depth and the existing layering are unchanged.
- **Muted text now passes.** `--ink-3` was #6C7484: 3.85:1 on the pane, 3.31:1 on elev-2 — under 4.5:1 everywhere it was used, and it is used for real text (hints, section headers, row meta, `.settled`, `.thinking`, the usage button). #9C938B clears 4.5:1 on every surface including `.sel-chip`'s orange-tinted ground, which was the last holdout at 4.22:1.
- **The focus ring is solid `--accent`.** `--accent-line` at 50% alpha blends to 2.37:1 against the pane, below the 3:1 a focus indicator needs; `:focus-visible` and the five focus borders (text input, composer box, path field, rules textarea, site filter) take the solid tone at 7.10:1. `--accent-line` stays for decorative borders.
- **`--err` is rose-leaning** (#FF8C9C, hue ≈352) rather than a plain red: at hue 0 it read as a dimmer accent next to the orange, which is exactly the confusion the toolchain checklist would cause — red "Not found" lines directly above an orange primary button. `--ok` #5BD1A0 and `--warn` #F5C15E were nudged to sit in the same warm-dark family. Diff add/del text and backgrounds follow their tokens.
- **The light surfaces followed too:** `--preview-bg` #E9E6E3, the preview overlay card's warm-neutral inks (#1F1C1A / #5C554E / #3E3833 on #F3F1EF), and its primary button on `--accent-2`. The three inline `#C0392B` error paragraphs in Preview.tsx became `className="danger"` → `.overlay .danger { color: #A8331C }` (6.66:1 on white), so no colour is hard-coded in a component any more.
- **`src-tauri/src/picker.js`** draws its selection outline and label in the brand orange (#FF5A3C outline at 3.10:1 on a white page, which is the 3:1 that non-text UI needs; label in `#1A0F0B`) instead of the old #2743D6.
- Verified in the mock (`pnpm dev`, driven from the browser) with a contrast auditor injected into the live DOM — it walks every element holding a text node, composes the real stacked background through alpha, and checks 4.5:1 (3:1 for large text): **zero failures** across the default session, a streaming turn with tool steps and an approval card waiting, Settings (both tabs), the Publish dialog with a target selected, the first-run checklist under `?tools=missing&sites=none`, the port-taken overlay, and full-width mode with the translucent floating panel and the usage popover open over a white page (worst case for the `color-mix` background) — lowest ratio 4.68:1. Diff rows, status chips, the banner, notices and the selection chip were audited from injected markup, since no mock state reaches them. `tsc`, `cargo build`, `pnpm test` (routes 29, transcript, 25 cargo tests) clean.

### Session 22 (2026-09-08) — the checklist says where to get things
- **Every external link in the checklist now opens.** They were plain `<a target="_blank">`, which does nothing in the Tauri webview, so "Install it from nodejs.org" was a dead end for the one person it was written for. Links go through a new `openExternal(url)` helper in src/backend.ts (the opener plugin, already in the capability list) via an `Ext` component in Checklist.tsx.
- **Node.js**: "Download the LTS installer from nodejs.org" pointing at nodejs.org/en/download, plus `brew install node` — but only when Homebrew is actually on the PATH (`Toolchain.has_brew`, one `which` call). A new amber state catches Node that is present but older than the starter can use (`nodeTooOld`, major < 20; the bundled starter is Next 16): "18.20.4 · too old", the same download line, and New site is disabled with "Needs a newer Node.js" instead of passing the check and failing at `next dev`.
- **git** says what it is for ("Supasito uses git for Undo and Publish") and splits the two failures: the `/usr/bin/git` stub gets `xcode-select --install` and a description of the dialog macOS opens; a genuinely absent git gets git-scm.com, or `brew install git`.
- **git with no identity is caught up front.** `git config --get user.email` read from the home folder (`Toolchain.git_identity`); unset shows an amber "2.51.0 · no name set" with both `git config --global` commands. It used to surface only as a failed Publish (sites.rs's "Git doesn't know who you are yet"), after the user had done the work.
- **Packages**: the pnpm hint carries `npm install -g pnpm` and a link to pnpm.io. It could never be seen before — a fix line on an `ok` row was hidden in the compact checklist, and the full one only appears when something else is missing; fix lines now always render.
- **Commands are click-to-copy.** `Cmd` copies on click and swaps to "Copied"; `copyText` in src/util.ts tries the synchronous textarea + `execCommand` route first (it works on the user's gesture without a permission prompt) and falls back to `navigator.clipboard`. If both are refused the handler selects the command text instead, so ⌘C still works — which needs `user-select: text` on the pill, since the app sets `user-select: none` on the body.
- `toolsMissing` stayed "something is missing"; the git-identity case is a new `toolsWarn`, so the welcome box still opens for it but reads "One of them needs a minute of setup, or Publish will stop later." rather than telling the user to get missing tools.
- Verified in the mock, driven from the browser, with new switches `?tools=oldnode|nogitpath|nogitid` and `&brew=no`: each row's text, the amber dot and detail, New site disabled on old Node, the pnpm hint visible in Settings → Interface's "On this Mac", the welcome box's two sentences, and no console errors. The nodejs.org link called `openExternal` with `https://nodejs.org/en/download` while the app window stayed on its own URL; a real click on a command pill showed "Copied". `tsc`, `pnpm test` (25 cargo tests, including a new assertion that this Mac's git identity is set), `cargo build` clean.
- Not verified in the Tauri window: `openUrl` reaching the browser and which of the two clipboard routes WKWebView takes (the embedded preview refuses both, which is how the select-the-text fallback got exercised).

### Session 24 (2026-09-08) — signed and notarized
- **Developer ID signing set up and exercised end to end.** The keychain had only an Apple Development certificate, which cannot sign for distribution. Key and CSR generated locally with openssl (`~/.private/supasito-signing/`, mode 600, outside the repo), the CSR uploaded to developer.apple.com, and the returned certificate is "Developer ID Application: Wojciech Wozniak (AR9C3X8J27)" issued by the G2 sub-CA, valid to 2031-09-09. Its public key matches the generated private key (same md5), so the pair is intact.
- **Gotcha worth keeping:** `security import` of a p12 built with an empty passphrase fails with "MAC verification failed during PKCS12 import (wrong password?)" — OpenSSL 3 defaults to algorithms the macOS importer will not read. Rebuilding with `openssl pkcs12 -export -legacy` and a real passphrase imported cleanly. Signing then ran without a keychain prompt blocking it.
- **Notarization by App Store Connect API key** (Team Keys, Developer role), referenced from `.env.release` as `APPLE_API_ISSUER` / `APPLE_API_KEY` / `APPLE_API_KEY_PATH`; the `.p8` lives beside the certificate key, never in git. `xcrun notarytool history` authenticated before the build, which is the cheap way to catch bad credentials without paying for a full compile.
- **The build.** `pnpm release --zip` at 0.1.0: signed with the hardened runtime (`flags=0x10000(runtime)`), a secure timestamp and `TeamIdentifier=AR9C3X8J27`, no entitlements — none are needed, since the app only spawns separate child processes and does not load third-party dylibs. Notarization came back Accepted (submission `648e8509-33e6-4afa-bde6-5d8e35487b74`) and the ticket was stapled. App 13 MB, zip 5.6 MB.
- **Acceptance, done the way a recipient experiences it:** the zip was unpacked to a scratch folder, `com.apple.quarantine` set on the copy by hand, and then `stapler validate` passed and `spctl --assess --type execute` answered `accepted`, `source=Notarized Developer ID`. So the README's `xattr` step is no longer needed for signed builds. PLAN §8.4 closed except that a second Mac still has not run it.
- **Hardened runtime does not break the app**, which was the open risk: installed to /Applications and launched, the signed binary spawned a `node` child and a dev server listening on 4323, the toolchain check reported Claude Code 2.1.257, and a turn's tool calls and the live preview rendered. No crash reports.
- `scripts/release.sh` now runs `xcrun stapler validate` after `codesign --verify`, because a signed-but-unstapled app still warns on a Mac that is offline at first launch, and the old script could not tell the two apart. Its header documents where the key material lives. Deleted the stale pre-rename `release/Open-0.1.0-macos.zip`.
- README, PLAN and CLAUDE.md no longer claim the app is unsigned; the install section drops the quarantine step and the troubleshooting entry for "damaged" now points at a corrupted download or a pre-0.1 build.
- **A version roadmap (PLAN §9)**, replacing the bare backlog list. Each version is a claim you can make honestly when it ends rather than a feature list: v0.1 installs, v0.2 is usable by someone who isn't you, v0.3 survives other people's projects, v0.4 is honest over a long session, v1.0 is recommendable. The §8 items are ordered by what blocks what, with the week on the release app first because nothing else can surface a fault nobody thought to test for. Auto-update moves out of the backlog into §8.7: it was deferred while there was nothing to distribute, and signing was its prerequisite. Windows and a second agent backend stay deferred in §9a with the §2 cut list.

### Session 25 (2026-09-08) — Escape leaves full width
- **Escape now leaves full-width preview.** ⌘\ and the arrows button in the floating panel were the only ways out, and neither is where a hand goes first. It sits at the end of App.tsx's Escape chain — after the diff, the rules sheet, the picker, Publish, New site, Remove site, the site menu, Settings and interrupting a running turn — so it only fires when there is nothing else to dismiss; with Settings open over the mode, the first press closes Settings and the second leaves full width. The chain also returns early on an already-handled event, because the composer marks its own Escape (closing the slash-command list) with `preventDefault`.
- **A key pressed inside the preview never reaches the app window**, so `picker.js` forwards Escape to the host as a `key` message and Preview.tsx acts on it — the one thing Escape can mean out there is "leave full width". The forwarder is a bubble-phase window listener that skips `defaultPrevented` events, so a previewed page that uses Escape for its own dialog has already handled or stopped it and keeps the key; the picker's own Escape (leaving pick mode) stays in the capture phase ahead of it.
- Both exit buttons' tooltips, the README's shortcut table and its full-width paragraph now say Esc.
- Verified in the mock, driven from the browser: with the composer focused, a real Escape press left the mode and brought the sidebar and conversation back; with Settings open the two presses went in the right order; a keydown dispatched inside the preview frame was forwarded and left the mode. `tsc` and `pnpm test` clean.
- Not verified by hand: a real keypress inside the preview frame. The browser driver's synthetic keys do not reach that frame's own listeners — the pre-existing "Escape leaves pick mode" does not fire from them either — so that path is only exercised by a dispatched event. One press in `pnpm tauri dev` would close it.

### Session 26 (2026-09-08) — the starter, made publishable
Audited `starters/next` against the Next.js production checklist by running the real path a new site takes (install → typecheck → build → `next start` → curl). It built clean and static, so nothing was broken; what it shipped was thin.

- **A new site now carries its own name.** `create_from_starter` personalised only `supasito.json` and `package.json`, so a site called "Acme Coffee" served `<title>New site</title>`, a header reading "New site" and a footer to match — confirmed in the rendered HTML before the fix. The starter gained `site.ts` (name, description, email, public URL) as the one place identity lives; layout, header, footer, the social card, the icon, `robots.txt` and `sitemap.xml` all read from it, and Rust rewrites that one line. `personalise_site_ts` escapes through `serde_json`, so a name with a quote or backslash cannot break the module; a unit test covers that and the real creation test now asserts no placeholder survives. Verified end to end: a site built as "Acme Coffee" has zero occurrences of "New site" in the page or the sitemap.
- **Four SEO endpoints that were 404 now answer 200.** `app/robots.ts`, `app/sitemap.ts`, `app/opengraph-image.tsx` (1200×630, drawn in the site's own paper and ink) and `app/icon.tsx` (32px, the site's initial). `metadataBase`, `title.template`, canonical, the full Open Graph set and a `summary_large_image` Twitter card are in the head — the previous head had only a title and a description, which is not enough for the "Ready to publish" card the starter itself shows.
- **The footer no longer prints the year.** `new Date().getFullYear()` in a prerendered Server Component is frozen at build time — the served HTML literally contained `© 2026` as a constant, so a site published in December would claim the wrong year for the following twelve months. Dropping it beats a client component for a copyright line, and CLAUDE.md now says why so an agent doesn't add it back.
- **Styled `not-found.tsx` and `error.tsx`.** `/nope` used to serve Next's unstyled black-on-white default next to a paper-and-ink design.
- **The display face is self-hosted via `next/font`** (Source Serif 4). The old stack led with "Iowan Old Style", which exists only on macOS: the owner approved a preview no Windows visitor would ever see. Fallbacks are kept, and the body stays on the system sans — no request to Google at runtime, font preloaded, no layout shift.
- **`pnpm-lock.yaml` is committed**, so a new site starts on the versions this was tested against instead of whatever `^16.3.4` resolves to that week. Two consequences handled: `create_from_starter` sets `CI=1`, which makes pnpm demand a matching lockfile and would fail site creation outright on an older pnpm, so the install passes `--no-frozen-lockfile`; and `rewrite_for_npm` deletes the lockfile for npm users, who cannot read it and would otherwise carry a stale file forever.
- Dropped the `Bash(pnpm lint*)` permission — there is no lint script and no ESLint. CLAUDE.md gained the `site.ts` map, the `next/image` rule (images go in `public/`, always sized, never a bare `<img>`), and the note to set the real domain before publishing.
- Verified: `pnpm test` (26 Rust tests), the ignored real-site-creation test, `tsc --noEmit` on the app, and the built starter served on `next start` — all seven routes static, the OG card and 404 checked visually in the browser.
- **Confirmed by hand afterwards** in the signed 0.1.0 build installed to /Applications: a site created through the app's own New-site dialog came out correct, which closes the one path the tests could not reach. That was the risk worth watching, because committing a lockfile changes how `pnpm install` behaves on the very first thing a new user does.

### Session 27 (2026-09-08) — a way to reply
- **"Got feedback?" is a third tab in Settings.** Nothing in the app asked for a reply: the trial runs on the installed build, papercuts get noticed at the keyboard, and there was no way to say so without leaving the app and looking up an address. The tab is one line and one button — no form, no field, nothing to fill in twice.
- **The button hands a `mailto:` to the default mail app**, with the subject filled in as "Supasito feedback" and the body left blank so people write in their own words. It goes through `openExternal` rather than a plain anchor, which in the Tauri webview opens nothing. No capability change was needed: `opener:default` already pulls in `allow-default-urls`, whose scope allows `mailto:*`, and the glob matches because the URL has no slash in it.
- Verified in the mock, driven from the browser: the tab renders and the button, with `window.open` stubbed, produced exactly `mailto:yo@wozu.co?subject=Supasito%20feedback`. `tsc` clean. Signed, notarized and installed to /Applications the same session.

### Session 28 (2026-09-08) — the preview pane stops dead-ending
First papercut from the release-app trial (PLAN §8c): opening `~/supasito` — the landing page, whose
package.json has scripts and no dependencies — showed "Dependencies aren't installed" over a button
that did nothing when clicked.

- **The install check now asks whether there is anything to install.** It was `package.json exists && node_modules missing`, which is true of every dependency-free manifest, and `npm install` on one of those creates no `node_modules` at all — so the button ran, succeeded, changed nothing, and the same card came back. It now also requires a non-empty `dependencies`, `devDependencies` or `optionalDependencies`. A unit test covers the plain-HTML case that started this.
- **The card's button prepares the site rather than running one command.** `prepareSite` installs, and if the site still cannot be previewed afterwards (nothing to install, or no dev command) it hands the folder to Claude with the setup prompt instead of leaving the user on a card whose only action has already been taken. If the install itself fails, the error stays on screen with "Try again" and "Ask Claude to fix it" next to it — an install failure is exactly the thing worth reading before retrying blindly.
- **"The dev server didn't start" is now "The preview couldn't start", with a second button.** Its only action was "Try again", which is not an answer when a dev command is wrong rather than unlucky — the site above would land there next, because its dev script hardcodes its port and cannot take the `--port` Supasito appends. `fixPreview` sends Claude the problem and the last 30 lines of output.
- **The copy assumes no technical background.** "Dependencies aren't installed" → "This site needs a few pieces first", explained as parts made by other people that aren't on the Mac yet, with the actual command relegated to a small line at the bottom for whoever wants it. The Node.js notes and the dev-server card were reworded the same way.
- **A repair asked for from the preview pane starts its own session** rather than appending to whatever conversation was open, since it is a different task from the one the user was in the middle of. An untouched draft is reused.
- Verified in the mock, driven from the browser: all three states (needs setup, install failed, dev server failed) render, and "Ask Claude to fix it" opened a fresh session carrying the failing output. `tsc`, `pnpm test` (28 Rust tests) clean. Signed, notarized and installed to /Applications the same session.

### Session 29 (2026-09-08) — Supasito stops inventing a port flag
The same site as session 28, one wall further on: the dev script serves with `python3 -m http.server 4173`,
and Supasito ran it as `npm run dev -- --port 4322`. Python takes its port as a positional argument and
exits on the flag. Supasito had made up a flag for a command it did not recognise.

- **A port flag is only appended for a framework we recognise.** Next, Astro, SvelteKit, Nuxt and Vite keep theirs, because there we know the flag exists. Any other `dev` script now runs as written (`npm run dev`), on whatever port it chooses; `PORT` is still in its environment for the servers that read it. This costs Supasito the ability to move such a site off a busy port, which the port-conflict card already explains, and it stops Supasito breaking setups that worked before it touched them.
- **`lsof` finds the server when its output cannot.** Dropping the flag alone would have traded an instant error for a 90-second hang: python announces its address on stdout, which is block-buffered behind our pipe and never arrives, and its request log never names the port. So when the port Supasito offered stays silent, every two seconds it asks the OS which TCP port the process group it spawned is listening on, and follows it. That is general — php, ruby, a hand-written node server, any `supasito.json` command without `{port}`.
- **A conflict Supasito cannot pin down no longer blames the wrong port.** `resolve_conflict` fell back to the port it had offered, so a server that died on *its own* port would have put a stranger's name on the port-conflict card. It returns `None` now, and the failure lands on the plain error card — which since session 28 has "Ask Claude to fix it" on it.
- The two Claude prompts no longer tell it that Supasito appends `--port <n>`, because for the scripts those prompts are about, it doesn't.
- Verified for real, not just in unit tests: `SUPASITO_SMOKE_SCENARIO=ports` run against the debug binary under a throwaway `HOME` (so the installed app's state and its running servers were untouched). A new case D builds the shape that started this — a package.json whose `dev` hardcodes a free port — and asserts the detected command is bare `npm run dev`, that the site reaches `ready` **on the port python chose**, and that the preview URL serves the file. All nine checks pass, the three pre-existing port-conflict cases included. Plus unit tests: `group_listen_port` against a real listener in this process's own group, and the detection rule.

### Session 31 (2026-09-10) — Codex as a second backend, on the `codex-backend` branch
The plan is CODEX.md; this session built its first three milestones. Every claim below was run through
the real app binary (debug build, `HOME=<empty dir> CODEX_HOME=~/.codex SUPASITO_PATH=$PATH`) against
codex-cli 0.149.0 with `SUPASITO_SMOKE_MODEL=gpt-5.6-luna`.

- **The model picker is the backend picker.** `src/models.ts` lists GPT-5.6 Terra/Sol/Luna next to the
  Claude models (ids from `model/list`); an OpenAI id makes `start_agent` spawn a Codex thread instead of a
  claude process (`is_codex_model` in codex.rs). Switching a running conversation across backends is refused
  with a toast — its history lives in the other CLI's store.
- **`src-tauri/src/agent/codex.rs`** speaks the app-server protocol (JSON-RPC 2.0 over stdio): one
  app-server per site hosting that site's threads, thread ids carried as `codex:<id>`, `agent://` events
  unchanged. `agent/mod.rs` gains the `Agent` enum so lib.rs has one code path; `AppState::agent(id)` picks.
  Approvals (`item/commandExecution/requestApproval`, `item/fileChange/requestApproval`) are shimmed into the
  Claude-shaped `can_use_tool` request so `Approval.tsx` renders them unchanged; the file-change one carries
  no diff, so the preceding `fileChange` item is joined by `itemId`. Every other server request is answered
  with a JSON-RPC error, since an unanswered one blocks the turn forever. Orphaned app-servers are matched by
  an argv marker (`-c supasito.site="<id>"`), never by name — the ChatGPT desktop app runs its own.
- **`src/agent/codex.ts`** turns notifications into the same `Item[]`: reasoning folds into the assistant
  bubble as thinking (needs `summary:"auto"` on `turn/start`, else it arrives empty), `fileChange.changes[].kind`
  gives Undo its `created` flag without the `agent://fs` probe, `commandExecution` is a Bash row,
  `thread/tokenUsage/updated` feeds the context ring with the real `modelContextWindow` (258 400 for Luna),
  `account/rateLimits/updated` feeds the plan rows, and **cost is null** — the protocol carries no dollars, so
  the row shows nothing rather than $0. Unknown item types render as a generic row. Fixture:
  `src/agent/fixtures/codex-0.149.0-write-turn.jsonl`, recorded by `scripts/codex-probe.mjs`.
- **Verified through the app:** a turn that writes a file and runs a command; both approval kinds asked for
  and answered through `agent_respond`; `turn/interrupt` ending a turn as `interrupted` at 6 s with the next
  turn working; a message sent mid-turn reaching Codex as `turn/steer` — it answers inside the same turn, so
  the smoke's `queue` scenario now checks for both replies in one `turn/completed` on Codex.
- **Toolchain and checklist:** `codex` located, versioned and asked `codex login status`; an optional Codex
  row on the checklist that never blocks.
- **Found the hard way:** a `match self.servers.lock().await.get(..)` scrutinee keeps its guard alive across
  the arm, and the arm locked the same mutex — a deadlock that showed as "start_agent never returns".
- **History (milestone 5).** `sessions_list` merges Codex's `thread/list` (filtered by the site's cwd, titled
  by the thread's first message, branch from `gitInfo`) with Claude's JSONL list; `session_transcript` replays
  a thread from `thread/read` as `item/started`/`item/completed` pairs plus a `turn/completed` per turn, so
  live and resumed use one reducer. A server spawned only for listing is shut down again when the site has no
  thread on it (270 MB is too much to keep for a list). Smoke `history` proves both through the app. The rail
  badges Codex sessions "GPT"; the footer names Codex and its version while one is on screen.
- **Site rules reach Codex.** It reads AGENTS.md, not CLAUDE.md, so a site with CLAUDE.md and no AGENTS.md
  gets its rules appended to the developer instructions (`with_site_rules`). Moving the dialog to AGENTS.md
  with `@AGENTS.md` in CLAUDE.md remains the long-term fix (CODEX.md §6).
- **Mock:** `pnpm dev` with `?agent=codex` replays the recorded fixture for a new session, pausing at both
  approvals until the card is answered; `?tools=nocodex|codexlogin` for the checklist. Caught one thing the
  reducer tests had not: Codex's `usedPercent` is 0–100 where Claude's utilization is 0–1 — the ring read
  "plan 1100%" until it was scaled.
- **Not yet (CODEX.md §8):** the Codex model list fetched live from `model/list` (three ids are hardcoded
  for now), AGENTS.md as the rules file, the deny reason as a steer and images as data URLs verified live.

### Session 30 (2026-09-08) — Supasito can update itself, and that is also the install count
There was no updater at all: someone who downloaded 0.1.0 would never learn 0.2.0 existed. Fixing that
also answers "how many users are there", because the honest way to count installs is a request the user
actually wants — a version check — rather than telemetry bolted on beside it.

- **`tauri-plugin-updater`, wired end to end.** `src-tauri/src/updates.rs` owns it: `update_check`,
  `update_install`, `update_dismiss` and `app_version` are the app's own commands, so nothing about the
  updater is reachable from the webview and the CSP is untouched. `Pending` holds what the last check
  found so Install does not ask twice.
- **Two endpoints, in order.** `supasito.com/updates/latest.json?v={{current_version}}&t={{target}}&a={{arch}}`
  first, `github.com/wojtekwoz/supasito/releases/latest/download/latest.json` second. Read from the plugin
  source (2.11.0): a non-2xx from the first endpoint is skipped silently and the loop moves on, so the site
  404ing today falls through to GitHub cleanly. A *200 with a non-JSON body* would abort the whole check
  instead — so `/updates/latest.json` must 404 or serve JSON, never a catch-all HTML page.
- **The count falls out of the check.** One check per install per day, so requests to that route per day are
  installs that ran that day, and `v` splits them by version. No identifier, no cookie, no event. Debug builds
  never check on their own (`#[cfg(not(debug_assertions))]`), so development cannot inflate the number;
  "Check now" still works in dev. WEBSITE.md §4.6 now describes downloads and installs as separate numbers.
- **The promise on the page had to change first.** README and WEBSITE.md §3 said the app "sends nothing
  anywhere". They now say what it sends and why, before launch, rather than being corrected after someone
  notices the traffic. Settings → Updates carries the same paragraph next to the switch that turns it off.
- **One surface, one line.** A row above the rail's foot when a newer version exists — "Supasito 0.2.0 is out",
  Update and restart, Not now — and a Settings tab with the version, Check now, the toggle and the disclosure.
  "Not now" is remembered per version, so a dismissed update does not come back until a later one does.
- **`pnpm release` signs the update and writes the manifest.** It refuses to start without the updater key
  (the build would otherwise fail late, or hang on a password prompt with no terminal), copies
  `Supasito.app.tar.gz` + `.sig` into `release/` and writes `release/latest.json` with only the arch it built
  for — an arm64 build must never be offered to an Intel Mac. Notes come from `release/notes-<version>.md`.
  The bundler wants the key in `TAURI_SIGNING_PRIVATE_KEY` (not `…_PATH`, which is the signer subcommand's)
  and `TAURI_SIGNING_PRIVATE_KEY_PASSWORD` set even when empty; both cost a failed build to find out.
- **The silent failure has a test.** If the key `pnpm release` signs with ever stops matching the pubkey in
  tauri.conf.json, every shipped copy refuses every future update, and nothing says so until it happens on
  someone else's Mac. `cargo test -- --ignored updater_package_verifies_against_the_shipped_pubkey` runs the
  same minisign check the app will run, against the artifacts the release just built.
- Verified: all three UI states driven in the browser against the mock (`?update=found`, `?update=error`,
  and nothing newer — banner, progress at 33%, both toasts), no console errors. `tsc` clean, `pnpm test`
  29 Rust tests. A full `pnpm release --zip` signed, notarized and stapled the app, produced a signed
  updater package, and the signature verifies against the shipped pubkey.
- **v0.1.0 is published**: <https://github.com/wojtekwoz/supasito/releases/tag/v0.1.0>, carrying the zip, the
  signed `Supasito.app.tar.gz` and its `.sig`, and `latest.json`. Every pinned asset URL answers 200, including
  the `download_url` inside the manifest that an updating copy will fetch.
- **The fallback endpoint is dead, and the release flag is why.** The release went out marked as a *prerelease*,
  and GitHub's `releases/latest` ignores prereleases: the repo has no "latest" release at all, so
  `releases/latest/download/latest.json` — the second endpoint every shipped copy tries — returns 404. Nothing
  about the build is wrong; one flag decides it. `gh release edit v0.1.0 --prerelease=false --latest` fixes it,
  and until then the app has no working endpoint at all, because the first one does not exist yet either.
- Still to do, and blocking the count rather than the updater: `supasito.com/updates/latest.json`. Until that
  file is served, every check falls through to GitHub, so the number that gets counted is nobody's — the
  fallback is there to keep updates working, not to measure them.

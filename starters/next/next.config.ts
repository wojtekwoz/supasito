import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Open embeds the dev server in an iframe on localhost; nothing special is needed for that.
};

export default nextConfig;

/**
 * Everything that identifies this site. Supasito writes `name` when the site is
 * created; the pages, the social card, robots.txt and sitemap.xml all read from here.
 */
export const site = {
  name: "New site",
  description: "A site built with Supasito.",
  email: "hello@example.com",
  /** The address the site will live at. Change it to the real domain before
   *  publishing — social cards and sitemap.xml need absolute links. */
  url: "https://example.com",
};
